# Usage example SimpleThreads

This is a small executable showcasing how to [find the package](CMakeLists.txt#L9) with cmake, [link against it](CMakeLists.txt#L16) and [include the headers](example.cpp#L1).

```{bash}
mkdir build
cd build
cmake .. . -G Ninja 
cmake --build .
```
